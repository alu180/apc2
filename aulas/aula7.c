#include <stdio.h>
#include <stdlib.h>

int *criar_vetor(int n_elementos, int inicializaçao) {
    int *vet = malloc(sizeof(int) * n_elementos);
    if (vet != NULL) {


    } 

    return vet;
}
void exibir_vetor(int n_elementos, int *vet) {
    if(n_elementos < 0 ) return;
    for(int i = 0; i < n_elementos; i++) {
        printf


    }


}

int main() {
    int *dynamic_num;



   return 0;
} 



